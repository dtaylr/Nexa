# Agent Plans

Use this folder for dated plans when work is multi-step, touches guarded paths,
or needs explicit human approval before implementation.

File name format:

```text
YYYY-MM-DD-short-topic.md
```

Each plan should include the problem, intended steps, likely files, verification
commands, and signoff points.
