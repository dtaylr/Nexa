meta {
  name: Register New User
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "newuser@1platform.dev",
    "password": "securepassword123",
    "role": "shopper"
  }
}

tests {
  test("Status 201 or 409 (already exists)", function() {
    expect([201, 409]).to.include(res.getStatus());
  });
}
